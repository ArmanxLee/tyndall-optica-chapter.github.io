function joinPlaceholder(event) {
  event.preventDefault();
  alert("The membership form will be added soon. Replace the # link in index.html with your Google Form URL.");
  return false;
}

function socialPlaceholder(event) {
  event.preventDefault();
  alert("Add the chapter's social-media URL in index.html.");
  return false;
}
